import React from 'react';

const SendButton:React.FC = () => {
    return (
        <div className={'wrapper_chat_page__block_button'}>
            <button className={'wrapper_chat_page__button'} >send</button>
        </div>
    );
};

export default SendButton;